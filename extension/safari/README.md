# Safari Extension Build
1. Open ExFetch.xcodeproj in Xcode
2. The Shared/ directory contains the same JS as the Chrome extension
3. Build and run the macOS app container
4. Enable the extension in Safari Preferences > Extensions
Note: Requires Xcode 15+ and macOS 14+.
